export interface DefaultTheme {
  breakpoints: {
    small: string;
    medium: string;
    large: string;
  };
  color: {
    textColor: string;
    backgroundColor: string;
    mainColor: string;
    subColor: string;
    pointColor: string;
  };
}
